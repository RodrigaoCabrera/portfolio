---
interface Props {
  title: string;
  isH1?: boolean;
}
const { title, isH1 }: Props = Astro.props;
---

{isH1 ? <h1>{title}</h1> : <h2>{title}</h2>}
<style>
  h1,
  h2 {
    font: var(--h2);
    color: var(--dark-color);
    margin: 0 0 8px;
  }
</style>
